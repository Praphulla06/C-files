#include <iostream>
#include <cmath>

int main()
{
    double x = 30.89;
    double y = 40;
    double z;

    //   z = std::max(x, y); // Max between two number
    //   z = std::min(x, y); // Min between two number
    //   z = std::pow(2,6); // power 2 raised to 6
    //   z = sqrt(49);
    //   z = abs(-90);
    //   z = round(x);
    //   z = ceil(x);
    //   z = floor(x);
    std::cout << z;
    return 0;
}